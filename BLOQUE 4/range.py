#print(list(range(1,13,3)))

#print(list(range(2500,2586)))

#print(list(range(3,301,3)))

suma_cuadrados = 0
valores = list(range(1,16))
for valor in valores:
    suma_cuadrados = suma_cuadrados + valor**2

print(suma_cuadrados)